# ERC-7730 Tools Environment Configuration
# ==========================================
# Copy this file to .env in the project root and fill in your API keys:
#   cp tools/env.example .env
#
# Usage:
#   Option 1: Source before running (no extra dependencies)
#     source .env && node tools/generate-tests.js registry/file.json
#
#   Option 2: Install dotenv and it loads automatically
#     npm install dotenv
#     node -r dotenv/config tools/generate-tests.js registry/file.json
#
#   Option 3: Use export directly in your shell
#     export ETHERSCAN_API_KEY=your-key
#     node tools/generate-tests.js registry/file.json
#
# IMPORTANT: Never commit .env files with real API keys!

# =============================================================================
# Block Explorer APIs (for fetching real transactions)
# =============================================================================

# Etherscan V2 API key - works for multiple chains via unified API
# Supported chains: Ethereum (1), BSC (56), Polygon (137), Arbitrum (42161),
#                   Optimism (10), Base (8453), Avalanche (43114)
# Get your key at: https://etherscan.io/apis
export ETHERSCAN_API_KEY=

# =============================================================================
# LLM APIs (for generating EIP-712 test examples)
# =============================================================================

# OpenAI API key
# Get your key at: https://platform.openai.com/api-keys
export OPENAI_API_KEY=

# Custom LLM endpoint (optional, defaults to https://api.openai.com)
# export LLM_BASE_URL=https://api.openai.com

# =============================================================================
# Azure OpenAI (alternative to standard OpenAI)
# =============================================================================

# Set to "true" to use Azure OpenAI API format
# export AZURE_OPENAI=true

# Azure OpenAI endpoint (full URL with deployment and API version)
# Example: https://YOUR-RESOURCE.openai.azure.com/openai/deployments/YOUR-DEPLOYMENT/chat/completions?api-version=2024-02-15-preview
# export LLM_BASE_URL=

# For Azure, use the same OPENAI_API_KEY variable for your Azure API key
# export OPENAI_API_KEY=your-azure-api-key
