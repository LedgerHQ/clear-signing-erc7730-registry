name: ✨ pull request

on:
  pull_request:
    paths-ignore:
      - 'developer-preview/**'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  ETHERSCAN_API_HOST: proxyetherscan.api.live.ledger.com

jobs:
  validate_descriptors:
    name: 🔎 validate descriptors
    runs-on: public-ledgerhq-shared-small
    timeout-minutes: 60
    steps:

      - name: Debug 1
        shell: bash
        run: curl 'https://proxyetherscan.api.live.ledger.com/api?module=contract&action=getabi&address=0x11111112542d85b3ef69ae05771c2dccff4faa26'

      - name: Debug 1
        shell: bash
        run: curl 'https://proxyetherscan.api.live.ledger.com/v2/api?module=contract&action=getabi&chainid=56&address=0x1111111254fb6c44bac0bed2854e76f90643097d'

      - name: Checkout
        timeout-minutes: 10
        uses: actions/checkout@v4

      - name: Get all changed descriptor files
        timeout-minutes: 5
        id: changed-descriptor-files
        uses: tj-actions/changed-files@v45
        with:
          files: |
            registry/**/eip712-*.json
            registry/**/calldata-*.json

      - name: Setup python
        timeout-minutes: 10
        if: steps.changed-descriptor-files.outputs.any_changed == 'true'
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install ERC-7730 library
        timeout-minutes: 10
        if: steps.changed-descriptor-files.outputs.any_changed == 'true'
        run: pip install erc7730

      - name: Validate ERC-7730 descriptors changed in pull request
        timeout-minutes: 10
        if: steps.changed-descriptor-files.outputs.any_changed == 'true'
        run: erc7730 lint ${{ steps.changed-descriptor-files.outputs.all_changed_files }} --gha
